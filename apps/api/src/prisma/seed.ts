import { prisma } from './prismaClient';

export const seedData = async () => {
  // await prisma.user.createMany({
  //   data: [{ email: 'foo@example.com' }, { email: 'bar@example.com' }],
  //   skipDuplicates: true,
  // });
};
